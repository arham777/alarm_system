import os

# Base alarm data directory (relative path)
ALARM_DATA_DIR = os.path.join(os.path.dirname(__file__), "ALARM_DATA_DIR")
PVCI_FOLDER = os.path.join(ALARM_DATA_DIR, "PVC-I (Jan, Feb, Mar) EVENTS")
